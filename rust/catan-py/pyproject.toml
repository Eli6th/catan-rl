[build-system]
requires = ["maturin>=1.5,<2"]
build-backend = "maturin"

[project]
name = "catan-py"
version = "0.1.0"
description = "Batched Catan RL environment (Rust engine, PyO3 bindings)"
requires-python = ">=3.9"
dependencies = ["numpy"]

[tool.maturin]
features = ["pyo3/extension-module"]
